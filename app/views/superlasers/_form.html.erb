<%= form_for(@superlaser) do |f| %>
  <% if @superlaser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@superlaser.errors.count, "error") %> prohibited this superlaser from being saved:</h2>

      <ul>
      <% @superlaser.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
